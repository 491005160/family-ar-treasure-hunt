# 家庭参考图

当前四张图片来自首轮家庭实拍，均已生成最长边 1600px 的网页副本；微信临时目录中的原图未被修改。

每个目标目前只有一张参考图。真实 matcher 接入前，建议为每个物品再补 2–4 张不同角度、不同光线下的照片，并同步维护 `app/core/targets.js` 中的 `referenceImages`。

当前项目仍运行 mock matcher，图片用于目标配置和界面展示，不会参与真实视觉判定。
